FROM postgres:16

# pg_partman: declarative partition 자동 생성/회수 도구.
# - Debian/Ubuntu 패키지명 형식: postgresql-<MAJOR>-partman
RUN apt-get update \
    && apt-get install -y --no-install-recommends postgresql-16-partman \
    && rm -rf /var/lib/apt/lists/*
